<div class="column is-7">
    <h3 class="title is-3">Messages</h3>
    <% @conversations.each do |conversation| %>
      <% if conversation.sender_id == current_user.id || conversation.recipient_id == current_user.id %>
        <% if conversation.sender_id == current_user.id %>
          <% recipient = User.find(conversation.recipient_id) %>
        <% else %>
          <% recipient = User.find(conversation.sender_id) %>
        <% end %>
        <% unless current_user.id == recipient %>
          <div class="columns">
            <div class="column">
              <div class="inline-block nudge-down-10"><%= gravatar_for recipient, size: 32 %></div>
              <div class="inline-block"><%= link_to recipient.username, conversation_messages_path(conversation) %></div>
               <% if conversation.recipient_id == current_user.id %> 
        <div class="column has-text-right">
         
          <%= link_to 'Delete', conversation, method: :delete, data: { confirm: 'Are you sure?' }, class: "button" %>

        </div>
        <% end %>
            </div>
          </div>
        <% end %>
      <% end %>
    <% end %>
  </div>
</br></br></br></br></br></br></br></br></br></br></br>
</br></br></br></br></br></br></br>
</div>